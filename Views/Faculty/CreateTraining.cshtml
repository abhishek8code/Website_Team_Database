@model TrainingAndWorkshop

<form asp-action="CreateTraining" method="post">
    @Html.AntiForgeryToken()

    <input type="hidden" asp-for="FacultyId" value="@ViewBag.FacultyId" />

    <div class="form-group">
        <label>Title</label>
        <input asp-for="Title" class="form-control" />
    </div>

    <div class="form-group">
        <label>Organized By</label>
        <input asp-for="OrganizedBy" class="form-control" />
    </div>

    <div class="form-group">
        <label>Date</label>
        <input asp-for="Date" class="form-control" />
    </div>

    <button type="submit" class="btn btn-success">Save</button>
    <a asp-action="Training"
       asp-route-id="@ViewBag.FacultyId"
       class="btn btn-secondary">
        Cancel
    </a>
</form>
